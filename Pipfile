[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-cors = "*"
werkzeug = "*"
gunicorn = "*"
pytest = "*"

[dev-packages]

[scripts]
start = "gunicorn app:app"
test = "pytest ."
coverage = "pytest --cov-report term-missing --cov=."

[requires]
python_version = "3.9"
